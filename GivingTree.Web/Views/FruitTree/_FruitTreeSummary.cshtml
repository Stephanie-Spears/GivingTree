@*@using Microsoft.AspNet.Identity*@
@model GivingTree.Web.Models.FruitTree

<div class="card">
@*	@if (Model.Files.Any(f => f.FileType == FileType.Photo))
	{
		<img style="max-width: 20rem;" class="card-img-top" src="~/File?id=@Model.Files.First(f => f.FileType == FileType.Photo).FileId" alt="photo"/>
	}*@
	<div class="card-body">
		<div class="card-header">
			<h3 class="card-title">@Model.Name</h3>
			<p class="card-text">@Model.Description</p>
		</div>


		@Html.ActionLink("Details", "Details", new {id = Model.Id})
		@Html.Encode(" | ")
		@Html.ActionLink("Edit", "Edit", new {id = Model.Id})
		@Html.Encode(" | ")
		@Html.ActionLink("Delete", "Delete", new {id = Model.Id})

		@*@if (Model.CreatedByUserId == User.Identity.GetUserId())
		{
			@Html.Encode(" | ")
			@Html.ActionLink("Edit", "Edit", new {id = Model.Id})
			@Html.Encode(" | ")
			@Html.ActionLink("Delete", "Delete", new {id = Model.Id})
		}*@

	</div>
</div>